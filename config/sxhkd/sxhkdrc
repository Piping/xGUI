# : for repeated chord
# ; for oneshot chord
# : ; cannot be used for same start sequence
# + for modifier combo

#xdotool key --window $(xdotool search --classname dwm) key alt+1

alt + r
 rofi -dpi 1 -show run  -theme ~/.config/rofi/themes/powermenu.rasi

super + x
 /usr/bin/bash ~/.config/rofi/scripts/powermenu.sh
